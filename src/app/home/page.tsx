import type { Metadata } from "next";
import { HomePage } from "@/views/home";

export const metadata: Metadata = {
  title: "금성위키",
  description: "금성고 학생·교사가 함께 쓰는 기록",
};

export default HomePage;
